# ⚠️ شرح أوضاع التشغيل (PAPER vs REAL)

## 🎯 الفرق بين PAPER Mode و REAL Mode

### 📝 PAPER Mode (الوضع التجريبي)
```
MODE=PAPER
```

**ماذا يحدث:**
- البوت يستخدم **بيانات تاريخية قديمة** (candles من الماضي)
- **الأسعار ليست حقيقية** - قد تكون من أيام/أسابيع سابقة
- **لا يوجد اتصال حقيقي** بالسوق الحالي
- مثال: إذا كان السعر الحقيقي في بينانس $69,580، قد يظهر في البوت $60,255 (من بيانات قديمة)

**الهدف:**
- اختبار منطق الكود (logic) فقط
- التأكد من عدم وجود أخطاء برمجية (errors)
- **لا يصلح لمعرفة الربحية الحقيقية**

**تحذير مهم:**
```
⚠️ الأرباح/الخسائر في PAPER Mode غير حقيقية!
⚠️ البيانات قديمة وليست Live
⚠️ لا يمكن الاعتماد عليها لتقييم الأداء
```

---

### 🔴 REAL Mode (التداول الحقيقي)
```
MODE=REAL
```

**ماذا يحدث:**
- البوت يستخدم **WebSocket للأسعار الحقيقية** (live prices)
- **الأوامر تنفذ فعلياً** في حسابك على Binance
- **رصيدك الحقيقي معرض للربح/الخسارة**
- الأسعار مطابقة تماماً لما تراه على بينانس

**متطلبات:**
```env
BINANCE_API_KEY=your_real_api_key
BINANCE_SECRET=your_real_secret
MODE=REAL
```

**تحذير الأمان:**
```
🚨 REAL Mode = أموال حقيقية!
🚨 تأكد من اختبار البوت جيداً في PAPER أولاً
🚨 ابدأ برأس مال صغير (100-500 USDT)
🚨 راقب البوت بشكل مستمر أول 24 ساعة
```

---

## 🐛 المشكلة التي حدثت عندك

### ما حصل:
1. البوت كان في `MODE=PAPER`
2. استخدام بيانات candles قديمة من وقت الانهيار ($60,255)
3. السعر الحقيقي الآن في بينانس ($69,580) - فرق $9,000!
4. أخطاء في منطق Stop Loss جعلته يغلق فوراً بـ "ربح وهمي"

### الإصلاحات:
✅ إصلاح منطق Stop Loss (كان مقلوباً)
✅ إصلاح منطق Take Profit
✅ إضافة تحذير واضح عند بدء PAPER Mode

---

## 🎯 التوصية

### للتجربة والاختبار:
```env
MODE=PAPER
TRADING_TYPE=futures
LEVERAGE=5
INITIAL_BALANCE=10000
```
**اعرف:** الأسعار ليست حقيقية، هذا فقط لاختبار الكود!

### للتداول الحقيقي:
```env
MODE=REAL
TRADING_TYPE=futures
LEVERAGE=3
INITIAL_BALANCE=500  # ابدأ صغير!
BINANCE_API_KEY=your_key
BINANCE_SECRET=your_secret
```
**تأكد:** API Key لديه صلاحيات Futures Trading

---

## 📊 كيف تعرف إذا البوت يستخدم بيانات قديمة؟

### علامات البيانات القديمة:
- ✅ السعر في البوت مختلف عن بينانس بمئات/آلاف الدولارات
- ✅ رسالة `⚠️ WebSocket timeout - cached data`
- ✅ صفقات تفتح وتغلق فوراً في نفس الثانية
- ✅ Stop Loss يحقق "أرباح" بدلاً من خسائر

### الحل:
1. أوقف البوت: `pm2 stop bot-trade-v1`
2. إذا كنت تريد **Live Testing**:
   ```env
   MODE=REAL  # غيّر إلى Real
   ```
3. أعد التشغيل: `pm2 restart bot-trade-v1`
4. تأكد من ظهور: `✅ WebSocket connections stable`

---

## ❓ أسئلة شائعة

**س: هل PAPER Mode يحتاج API Key؟**
ج: لا، لكن بدون API Key لن يكون له اتصال حقيقي بالسوق (يستخدم بيانات عامة فقط).

**س: لماذا الأرقام في PAPER Mode مختلفة عن بينانس؟**
ج: لأنه يستخدم candles تاريخية (ماضي) وليس أسعار live.

**س: كيف أختبر البوت بدون مخاطرة؟**
ج: استخدم **Binance Testnet** (شبكة اختبار بينانس) مع MODE=REAL.

**س: ما هو أفضل leverage للمبتدئين؟**
ج: 2x-3x فقط! لا تستخدم 5x+ إلا إذا كنت خبيراً.

---

**تاريخ التحديث:** 2025-02-10  
**الإصدار:** v2.0.1 (بعد إصلاح Stop Loss)
