module.exports = {
  apps: [
    {
      // ═══════════════════════════════════════════════════════
      // 🤖 BOT-TRADE-V1 Configuration
      // ═══════════════════════════════════════════════════════

      // تفاصيل التطبيق
      name: "bot-trade-v1",
      script: "./live-trader-ai-advanced.js",
      instances: 1,
      exec_mode: "cluster",

      // ═══════════════════════════════════════════════════════
      // 🌍 البيئات (Environments)
      // ═══════════════════════════════════════════════════════

      // البيئة الأساسية (PAPER Mode - محاكاة)
      env: {
        NODE_ENV: "production",
        MODE: "PAPER", // ← شغل محاكاة (آمن 100%)
      },

      // البيئة الإنتاجية (REAL Mode - تداول حقيقي)
      env_production: {
        NODE_ENV: "production",
        MODE: "REAL", // ← تداول حقيقي (احذر!)
      },

      // ═══════════════════════════════════════════════════════
      // 💾 إدارة الموارد (Resource Management)
      // ═══════════════════════════════════════════════════════
      max_memory_restart: "500M", // أعد التشغيل إذا وصل لـ 500MB

      // ═══════════════════════════════════════════════════════
      // 🔄 إعادة التشغيل الآلي (Auto Restart)
      // ═══════════════════════════════════════════════════════
      min_uptime: "10s", // البوت يجب يشتغل 10 ثواني على الأقل
      max_restarts: 10, // محاولات إعادة تشغيل
      restart_delay: 4000, // 4 ثواني بين كل محاولة
      autorestart: true, // إعادة تشغيل تلقائي عند الفشل

      // ═══════════════════════════════════════════════════════
      // 📋 السجلات (Logging)
      // ═══════════════════════════════════════════════════════
      log_date_format: "YYYY-MM-DD HH:mm:ss Z",
      out_file: "./logs/pm2-out.log", // stdout (طباعة عادية)
      error_file: "./logs/pm2-error.log", // stderr (أخطاء)
      merge_logs: true, // دمج كل السجلات معاً

      // ═══════════════════════════════════════════════════════
      // 👁️ المراقبة (Watching)
      // ═══════════════════════════════════════════════════════
      watch: false, // لا تراقب تغييرات الملفات (أسرع)
      ignore_watch: [
        "node_modules", // تجاهل المكتبات
        "logs", // تجاهل السجلات
        ".env", // تجاهل ملف البيئة
      ],

      // ═══════════════════════════════════════════════════════
      // 🛑 الإغلاق النظيف (Graceful Shutdown)
      // ═══════════════════════════════════════════════════════
      kill_timeout: 5000, // 5 ثواني قبل الإجبار على الإيقاف
    },
  ],
};
